import { StyleSheet } from 'react-native';
export default StyleSheet.create({
  timer: {
    flex: 1,
    alignSelf: 'flex-start',
    color:'#1ac92e',
    fontWeight:'bold',
    fontSize: 20,
  }
});
